/** Size of a single array cell or variable cell (square). */
export const CELL_SIZE = 48

/** Gap between adjacent cells in an array row. */
export const CELL_GAP = 2

/** Height of the array label above cells. */
export const ARRAY_LABEL_HEIGHT = 8

/** Height of the index labels below cells. */
export const INDEX_LABEL_HEIGHT = 22

/** Gap between the bottom of one array group and the top of the next. */
export const ARRAY_GROUP_GAP = 20

/** Left margin for content. */
export const CONTENT_X = 40

/** Top margin for the first array. */
export const CONTENT_Y = 60

/** Space reserved above arrays for pointer arrows. */
export const POINTER_SPACE = 60

/** Gap between the last array and the call stack. */
export const CALLSTACK_GAP = 16

/** Indentation per nesting level in call frames. */
export const FRAME_INDENT = 16

/** Height of the frame header line. */
export const FRAME_HEADER_HEIGHT = 28

/** Height of the array-ref labels row. */
export const FRAME_REFS_ROW_HEIGHT = 32

/** Frame top padding. */
export const FRAME_PADDING_TOP = 8

/** Frame bottom padding. */
export const FRAME_PADDING_BOTTOM = 12

/** Height of the down-arrow between nested frames. */
export const FRAME_ARROW_HEIGHT = 24

/** Frame border radius. */
export const FRAME_BORDER_RADIUS = 8

/** Gap between variable cells. */
export const VARIABLE_GAP = 20

/** Height of the variable label above the cell. */
export const VARIABLE_LABEL_HEIGHT = 8

/** Vertical gap between pointer arrow tiers. */
export const ARROW_Y_GAP = 18
